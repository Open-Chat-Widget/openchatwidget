# Objective 
I want to update our documentation to show how agents can be built that support web search and support MCP. 

# Task 
1. Create a new page called "Agent with web search" 
2. Create a new page called "Agent with MCP"

In the Agent with web search, explain how how to build an AI-SDK agent that supports web search, how pricing works, and how web search works. Also explain what that looks like in Open Chat widget 

In Agent with MCP, explain how to build an AI-SDK agent that is connected to an MCP server. Use MCPJam SDK. 

I created a working example of web search and MCP at sdk/sandbox. Use that project to get context. 

# Where
Our Mintlify documentation lives in docs